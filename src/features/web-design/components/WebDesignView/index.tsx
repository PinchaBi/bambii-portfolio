import Wrapper from "@/components/layout/Wrapper";

import WebDesignList from "../WebDesignList";

const WebDesignView = () => {
  return (
    <Wrapper height="75vh" paddingY={5} alignItems="flex-end">
      <WebDesignList />
    </Wrapper>
  );
};

export default WebDesignView;
